pages:
# NOTE: `name` field must match the `navbar_title` field in the page's front matter to ensure the correct page is highlighted in the navbar.
#       For example, the `navbar_title` field in `showcase.html` is "Beyond Research" and the `name` field here should also be "Beyond Research"
- name: Home
  url: /
- name: Publications
  url: /publications
#- name: Blog
  #url: /blog
- name: Beyond Research
  url: /showcase
